/**
 * Word store module barrel — public API for the in-memory, serializable
 * word store (PRD §5.2, §6.4) and its progress metrics (PRD §5.9).
 */

export { WordStore, progressMetrics, primaryStoreLang } from "./wordStore.js";
